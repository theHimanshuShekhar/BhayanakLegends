export function CaveatFooter() {
  return (
    <p className="text-[10px] text-dimmer" data-testid="population-caveat">
      Pack numbers are learned from the shipped population corpus; your Personal History never
      mixes into them.
    </p>
  );
}
