export function EventFeed() {
  return (
    <section className="rounded-lg border border-line bg-surface p-4 shadow-z1" data-testid="event-feed">
      <div className="text-[10px] uppercase tracking-widest text-accent">Event feed</div>
      <div className="mt-3 rounded-md border border-dashed border-line bg-deep px-3 py-6 text-center text-xs text-dim">
        event feed lands with LCU bridge
      </div>
    </section>
  );
}
