name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    name: Required repository gates
    uses: ./.github/workflows/verify.yml
    with:
      dry_run: false
      simulate_failure: false
    permissions:
      contents: read
